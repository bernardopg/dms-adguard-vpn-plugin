# AdGuard VPN Plugin v1.3.0

> **Language / Idioma:** English is primary. Portuguese (Brazil) translation follows.

This release introduces a major UI refresh, runtime connection hardening, and a broad multilingual expansion.

## Highlights

- Full redesign of the popout and settings surfaces with clearer hierarchy and faster action flow.
- Added runtime preflight checks before connect to handle stale socket and busy runtime states.
- Added explicit guard for multi-default-route scenarios in TUN mode.
- Expanded localization coverage to 22 locales with a safe fallback strategy.
- Upgraded i18n validation tooling to check all locale files and report fallback coverage.

## Compatibility

- Requires DankMaterialShell `>= 1.4.0`.
- Requires `adguardvpn-cli` available in PATH (or configured binary path).

## Notes

- Previous release: `v1.2.0`

---

## Português (Brasil)

Esta versão traz uma grande atualização visual, endurecimento da conexão em runtime e expansão multilíngue.

### Destaques

- Redesign completo do popout e da tela de settings.
- Preflight de runtime antes de conectar para lidar com socket obsoleto e runtime ocupado.
- Proteção explícita para múltiplas rotas default em modo TUN.
- Expansão de localização para 22 locales com fallback seguro.
- Ferramenta de validação i18n atualizada para verificar todos os locale files.

### Compatibilidade

- Requer DankMaterialShell `>= 1.4.0`.
- Requer `adguardvpn-cli` no PATH ou caminho configurado.

### Notas

- Release anterior: `v1.2.0`
