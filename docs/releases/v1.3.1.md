# AdGuard VPN Plugin v1.3.1

> **Language / Idioma:** English is primary. Portuguese (Brazil) translation follows.

This hotfix release improves account detection reliability and diagnostics in environments where `adguardvpn-cli license` can be slow.

## Highlights

- Increased reliability of account detection by hardening the license refresh flow.
- Added a safer timeout strategy for slow `license` calls plus an in-flight watchdog.
- Expanded license parsing patterns for alternate output formats.
- Preserved valid account metadata when transient/partial output occurs.
- Ensured diagnostics always show the latest attempted command, including preflight failures.
- Fixed popup compatibility by removing unsupported `selectByMouse` usage in the DNS field.
- Updated PT-BR copy for the multiple-default-route warning to "rotas padrão".

## Compatibility

- Requires DankMaterialShell `>= 1.4.0`.
- Requires `adguardvpn-cli` available in PATH (or configured binary path).

## Notes

- Previous release: `v1.3.0`

---

## Português (Brasil)

Esta hotfix melhora a confiabilidade de detecção da conta e os diagnósticos em ambientes onde `adguardvpn-cli license` pode ser lento.

### Destaques

- Melhorada a confiabilidade da detecção de conta.
- Adicionada estratégia de timeout mais segura para chamadas lentas de `license`.
- Expandidos os padrões de parsing de licença.
- Preservados metadados válidos quando a saída é parcial ou transitória.
- Diagnósticos agora mostram o último comando tentado, inclusive falhas de preflight.
- Corrigida compatibilidade do popout removendo uso não suportado de `selectByMouse` no campo DNS.
- Atualizada a cópia PT-BR para “rotas padrão”.

### Compatibilidade

- Requer DankMaterialShell `>= 1.4.0`.
- Requer `adguardvpn-cli` no PATH ou caminho configurado.

### Notas

- Release anterior: `v1.3.0`
